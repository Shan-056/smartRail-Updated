# Dependencies — everyone regenerates this locally via "npm install"
node_modules/

# Next.js build output
.next/
out/

# Secrets — never commit real environment variables
.env
.env.local
.env.*.local

# TypeScript incremental build cache
*.tsbuildinfo
next-env.d.ts

# Common OS/editor junk files
.DS_Store
Thumbs.db
*.log
