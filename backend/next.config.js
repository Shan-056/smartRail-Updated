/** @type {import('next').NextConfig} */
const nextConfig = {
  // "standalone" produces a minimal, self-contained build folder —
  // makes free-tier deployment (e.g. Render, Railway) simpler later.
  output: "standalone",
};

module.exports = nextConfig;
